{
  "name": "{{PROJECT_SLUG}}",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nullstack": "~0.15.0",
    "tailwindcss": "^3.0.0",
    "concurrently": "^7.2.0"
  },
  "scripts": {
    "start:nullstack": "npx nullstack start --mode=spa --hot",
    "start:tailwind": "npx tailwindcss -o ./tailwind.css --watch",
    "start": "concurrently -k -r npm:start:*",
    "build:nullstack": "npx nullstack build --mode=ssr",
    "build:tailwind": "npx tailwindcss -o ./tailwind.css",
    "build": "npm run build:tailwind && npm run build:nullstack"
  }
}